<% if user_signed_in? %>
    <% if current_user.admin? %>
        <h1>New Category</h1>

        <%= render 'form', category: @category %>
    <% end %>
<% end %>
        
<%= link_to 'Back', categories_path %>
